name: 🔄 ETL Pipeline (S3 -> RDS)

on:
  schedule:
    - cron: '0 2 * * *'  # Agendado para 2:00 AM UTC diariamente
  workflow_dispatch:      # Gatilho manual para testes

jobs:
  etl_process:
    name: 🏭 Run ETL Data Pipeline
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Checkout Repository
        uses: actions/checkout@v4

      - name: 🐍 Setup Python 3.11
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: 📦 Create Lambda Deployment Package
        run: |
          mkdir -p package
          
          # 1. Instalar dependências (boto3 já existe na Lambda AWS, removendo para economizar espaço)
          pip install \
            --platform manylinux2014_x86_64 \
            --target ./package \
            --implementation cp \
            --python-version 3.11 \
            --only-binary=:all: --upgrade \
            pandas sqlalchemy psycopg2-binary

          # 2. Copiar script ETL e SQL Schema
          cp nexus-sus-etl/extrair_sus.py ./package/
          cp nexus-sus-etl/create_table.sql ./package/
          
          # 3. Zipar tudo
          cd package
          zip -r ../deployment_package.zip .
          cd ..
          
          echo "📦 Pacote ZIP criado: deployment_package.zip"
          du -h deployment_package.zip

      - name: 🚀 Deploy & Invoke Lambda
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          AWS_REGION: ${{ secrets.AWS_REGION }}
        run: |
          # Define Environment logic
          if [[ "${{ github.ref }}" == "refs/heads/main" ]]; then
            ENV="production"
            LAMBDA_NAME="nexus-sus-etl-production"
          else
            ENV="staging"
            LAMBDA_NAME="nexus-sus-etl-staging"
          fi
          
          BUCKET_NAME="nexus-sus-data-lake-${ENV}"
          S3_KEY="artifacts/lambda/etl-${{ github.sha }}.zip"
          
          echo "🚀 Config: Lambda=$LAMBDA_NAME | Bucket=$BUCKET_NAME"
          
          # 1. Upload para S3 (Bypass no limite de 50MB direct upload)
          echo "📤 Uploading package to s3://$BUCKET_NAME/$S3_KEY"
          aws s3 cp deployment_package.zip s3://$BUCKET_NAME/$S3_KEY
          
          # 2. Update Lambda Code via S3
          echo "🔄 Updating Function Code..."
          aws lambda update-function-code \
            --function-name $LAMBDA_NAME \
            --s3-bucket $BUCKET_NAME \
            --s3-key $S3_KEY

          echo "⏳ Waiting for update..."
          sleep 10
          
          echo "⚡ Invoking Lambda..."
          aws lambda invoke \
            --function-name $LAMBDA_NAME \
            --cli-binary-format raw-in-base64-out \
            --payload '{}' \
            response.json
            
          echo "📄 Lambda Output:"
          cat response.json
          
          if grep -q "ErrorMessage" response.json; then
            echo "❌ ETL Falhou!"
            exit 1
          fi

      - name: ✅ Notify Success
        if: success()
        run: echo "ETL (Lambda) Concluído com Sucesso!"

      - name: ❌ Notify Failure
        if: failure()
        run: echo "ETL Falhou. Verifique os logs da Lambda no CloudWatch."
